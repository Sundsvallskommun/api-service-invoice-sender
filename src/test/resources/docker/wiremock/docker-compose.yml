services:
  wiremock:
    container_name: wiremock
    image: wiremock/wiremock:latest
    ports:
      - "9090:8080"
    volumes:
      - ./:/home/wiremock
    command:
      - --verbose
      - --global-response-templating
    restart: no


